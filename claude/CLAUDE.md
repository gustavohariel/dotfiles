# Global preferences

- If a CLI tool is available for a service (e.g. `tea`, `acli`, `posthog-cli`, `ol`), prefer it over the equivalent MCP server. Only fall back to an MCP tool when no CLI covers what's needed.
